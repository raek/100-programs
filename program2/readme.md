# Erlang BEAM File Parser

This is a script that parses parts of the .beam file format, which is
what Erlang and Elixir source files compile into. They contain
bytecode for the Erlang VM, called BEAM.
